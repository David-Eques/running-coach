# Strava API app credentials — from https://www.strava.com/settings/api
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Strong random token. Used by Claude Code to auth into the MCP.
# Generate with: openssl rand -hex 32
MCP_BEARER_TOKEN=
